<% layout('./layout/page.ejs') %>

<style>
    body {
        padding: 40px;
        font: 20px "Didot", "Bodoni MT", "Times New Roman", serif;
        background-color: #fbc8d9 !important; /* Светло-розовый фон */
        line-height: 1.6;
        letter-spacing: 0.5px;
    }

    .container {
    max-width: 600px; /* Максимальная ширина контейнера */
    margin: 50px auto; /* Центрирование */
    padding: 20px; /* Отступы внутри контейнера */
    background-color:#ffc8db; 
    border-radius: 10px; /* Закругленные углы */
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1); /* Тень для контейнера */
    }

    h1 {
        text-align: center;
        color: #d5006d; /* Розовый заголовок */
    }

    p {
        text-align: center;
        color: #555;
    }

    .form-group {
        margin-bottom: 15px;
    }

    .control-label {
        color: #d5006d; /* Розовая метка */
    }

    .form-control {
        border: 1px solid #d5006d; /* Розовая рамка для полей ввода */
        border-radius: 5px;
    }

    .form-control:focus {
        border-color: #ff4081; /* Более яркая розовая рамка при фокусе */
        box-shadow: 0 0 5px rgba(255, 20, 147, 0.5);
    }

    .btn-primary {
        background-color: #d5006d; /* Розовая кнопка */
        border-color: #d5006d; /* Розовая рамка кнопки */
        transition: background-color 0.3s;
    }

    .btn-primary:hover {
        background-color: #ff4081; /* Более яркий цвет при наведении */
        border-color: #ff4081;
    }
</style>

<div class="conteiner">
    <h1>Регистрация и вход</h1>
    <p>Введите имя пользователя и пароль, если такого пользователя нет, то он будет создан.</p>
    <form class="form-horizontal" action="/logreg" method="post">
        <div class="form-group">
            <label class="control-label col-sm-1">Имя</label>
            <div class="col-sm-5">
                <input class="form-control" type="text"
                                   placeholder="Введите имя" name="username"/>
            </div>
        </div>
        <div class="form-group">
            <label class="control-label col-sm-1">Пароль</label>
            <div class="col-sm-5">
                <input class="form-control" type="password"
                                 placeholder="Введите пароль" name="password"/>
            </div>
        </div>
        <br/>
        <div class="form-group">
            <div class="col-sm-offset-1 col-sm-5">
                <input class="btn btn-primary" type="submit"
                                             value="Войти"/>
            </div>
        </div>
        <% if(error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>            
    </form>
</div>